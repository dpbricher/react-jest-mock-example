# React Jest Mock Example

Very basic example of mocking a module in a React component using Jest

## Prerequisites

Node.js and npm

## Building

Run `npm install`

## Running

You can use either `npm test` or `npx jest`
